# Linked design skill (symlink into a local checkout; install per-machine)
.claude/skills/impeccable
# Local impeccable state
.impeccable/config.local.json
# Impeccable build and review captures (large PNGs, regenerated per session)
.impeccable/review/
.impeccable/build/
.impeccable/mocks/
